# Project progress log

This document tracks what has actually been implemented, phase by phase, so
a new session (human or AI) can resume the work without having to rebuild
context from scratch. It is updated at the end of each completed phase.

For the full architecture (data model, technical decisions, planned phases),
see [`docs/plan.md`](plan.md) — this file is the execution log; that one is
the design reference.

## Overall status

| Phase | Description | Status |
|---|---|---|
| 1 | Skeleton (Rails + Docker + Devise + Pundit + Roles) | ✅ Done |
| 2 | Driver/truck registration | ✅ Done |
| 3 | Yard check-in (`Visit` model) | ⏳ Not started |
| 4 | Dispatch screen (order issuance) | ⏳ Not started |
| 5 | Queue screen (finish loading) | ⏳ Not started |
| 6 | Public real-time screen (Turbo Streams) | ⏳ Not started |
| 7 | Notifications (SMS/WhatsApp via Twilio) | ⏳ Not started |
| 8 | Admin panel (Avo) | ⏳ Not started |
| 9 | Test coverage + styling | ⏳ Not started |
| 10 | Production hardening | ⏳ Not started |

---

## Phase 1 — Skeleton (done)

**Commit**: `7a94d54` — "Phase 1: Rails skeleton with Devise, Pundit, roles and Docker setup" (branch `main`)

### What was created

- **Git**: repository initialized at `D:\Sites`, branch `main`. Commit
  identity configured locally for this repo as
  `Fabricio Albarnaz <fabricio.albarnaz@gmail.com>` (repo-local git config,
  not global).
- **`docs/plan.md`**: full architecture plan, in English.
- **Rails 8.0.5 app** (Ruby 3.3.11), generated with
  `rails new . --database=postgresql --skip-test` inside a temporary Docker
  container (no local Ruby install available). Plain CSS + importmap (no
  Node/Yarn).
- **Two Dockerfiles**:
  - `Dockerfile` — the production one, generated by Rails itself (multi-stage,
    non-root user). Adjusted: removed the `thruster` gem dependency (CMD is
    now plain `./bin/rails server`, `EXPOSE 3000`).
  - `Dockerfile.dev` — used by `docker-compose.yml`, installs every gem group
    (including test), Chromium/chromium-driver (for Cuprite system specs),
    runs with the source mounted as a volume.
- **`docker-compose.yml`**: services `db` (postgres:16-alpine), `redis`
  (redis:7-alpine), `web` (rails server, port 3000), `worker` (sidekiq).
  Healthchecks on `db`/`redis`; named volumes `pgdata`, `redisdata`,
  `bundle_cache`.
- **`.env.example`** (version-controlled) and **`.env`** (gitignored) with the
  Twilio variables (empty for now — only used starting in Phase 7).
- **Gemfile**, adjusted from the Rails 8 default:
  - Removed: `solid_queue`, `solid_cable` (we chose Redis+Sidekiq instead),
    `kamal`, `thruster` (not part of our deploy plan).
  - Kept: `solid_cache` (Rails cache store, unrelated to the jobs decision).
  - Added: `devise`, `devise-i18n`, `pundit`, `avo`, `sidekiq`, `redis`,
    `twilio-ruby`, `phonelib`, `cpf_cnpj`, `dotenv-rails`, `rspec-rails`,
    `factory_bot_rails`, `faker`, `shoulda-matchers`, `pundit-matchers`,
    `capybara`, `cuprite`, `webmock`, `vcr`.
- **Authentication / Authorization**:
  - `User` (Devise: `database_authenticatable`, `recoverable`, `rememberable`,
    `validatable`, `trackable`, `lockable` — **no** `:registerable`; users are
    only created by an admin via Avo). `name` is a required field.
  - `ApplicationPolicy` (Pundit) generated, no resource-specific policies yet.
- **Roles (`Role`/`UserRole`)**:
  - `Role`: `key` (unique, restricted to `admin`/`cadastro`/`expedicao`/`fila`
    via `Role::KEYS`), `name`.
  - `UserRole`: join on `user_id`+`role_id`, unique.
  - `User#role?(key)` and `User#admin?` helper methods.
- **Seeds** (`db/seeds.rb`): creates the 4 roles + 1 admin user. Credentials
  come from `ENV["ADMIN_EMAIL"]`/`ENV["ADMIN_PASSWORD"]`, falling back to
  `admin@example.com` / `changeme123` in dev.
- **i18n**: `default_locale` and `available_locales` set to `:"pt-BR"` in
  `config/application.rb`. `config/locales/pt-BR.yml` created (minimal for
  now). `devise-i18n` already ships Devise's own translations.
- **`HomeController`** (the `root` route) — a minimal, temporary authenticated
  page that only proves login works; shows the logged-in user's name/email/
  roles. **Will be replaced by the real screens in later phases** (each role
  gets its own screen/namespace: `cadastro/`, `expedicao/`, `fila/`,
  `public/`).
- **Testing**: `rspec-rails` installed (`spec/rails_helper.rb`,
  `spec/spec_helper.rb`), `FactoryBot::Syntax::Methods` and
  `shoulda-matchers` wired up in `rails_helper.rb`. Model specs for `User`,
  `Role`, `UserRole` (14 examples, all passing). The `Role` factory uses
  `initialize_with { Role.find_or_initialize_by(key: key) }` because only 4
  valid keys exist (fixed reference data — "creating" another one doesn't
  make sense).

### Deviations / decisions discovered during execution (not in the original plan)

- **Avo resolved to v4.x**, not 3.x as the original plan assumed (no version
  was pinned in the Gemfile). Avo 4's configuration/authorization syntax may
  differ from what's documented in the plan — **revisit in Phase 8**.
- Avo automatically generates a resource (`app/avo/resources/*.rb`) and a
  controller (`app/controllers/avo/*_controller.rb`) for every model created
  via `rails generate model`, even though the Avo admin panel itself hasn't
  been mounted yet. This is convenient (it gets a head start on Phase 8), but
  it means these files already exist and will keep growing with every new
  model — worth reviewing/organizing once we reach Phase 8.
- Docker bind mount on Windows/Git Bash: `-v "$(pwd):/app"` **does not work**
  (it silently creates an anonymous volume instead of mounting the host
  directory) — the Windows-style path must be used instead
  (`-v "D:/Sites:/app"`) together with `MSYS_NO_PATHCONV=1`. Already handled
  in every command used so far; documented here to avoid hitting it again.
- Git identity was not configured in this environment (neither locally nor
  globally) — configured locally for this repo only (see above).

### How to run / verify

```bash
# bring up the full stack
docker compose up -d

# prepare the dev database
docker compose run --rm web bin/rails db:prepare
docker compose run --rm web bin/rails db:seed

# prepare the test database and run specs
docker compose run --rm -e RAILS_ENV=test web bin/rails db:prepare
docker compose run --rm -e RAILS_ENV=test web bundle exec rspec

# visit http://localhost:3000 — log in with admin@example.com / changeme123
# (or your own ADMIN_EMAIL/ADMIN_PASSWORD, if customized in .env)
```

Verified end-to-end during this phase: `docker compose up` boots
`web`+`worker`+`db`+`redis` with no errors; admin login through the Devise
form works (POST `/users/sign_in` → redirect → authenticated home page
showing name and roles); RSpec suite has 14 examples, 0 failures.

---

## Phase 2 — Driver/truck registration (done)

### What was created

- **ActiveRecord::Encryption**: keys generated via `bin/rails db:encryption:init`
  and stored in `config/credentials.yml.enc` (single file, not split per
  environment — same keys used in dev/test/production). Needed before the
  `Driver` model could exist, since `cpf`/`phone` are encrypted at rest.
- **Models**:
  - `Driver`: `name`, `cpf` (encrypted, deterministic — required for the
    uniqueness validation and unique DB index to work against ciphertext),
    `phone` (encrypted, deterministic), `notification_channel` enum
    (`sms`/`whatsapp`/`both`, string-backed, default `sms`), `active`
    (boolean, default `true`). CPF format validated with `CPF.valid?` from
    `cpf_cnpj` (the gem has no built-in Rails validator, unlike assumed —
    just the `CPF`/`CNPJ` classes). Phone validated via `phonelib`'s
    `phone: true` validator (E.164).
  - `Truck`: `plate` (unique, normalized to upcase/stripped via Rails'
    `normalizes`), `model`, `capacity` (validated `> 0`, nil allowed),
    `active`.
  - `DriverTruck`: join model, unique on `[driver_id, truck_id]`.
  - `has_many :trucks, through: :driver_trucks` on `Driver` (and the
    inverse on `Truck`) — this gives a free `truck_ids=` writer, used by
    the driver form's checkbox list to manage pairings.
- **Policies**: `DriverPolicy`, `TruckPolicy` — both allow
  index/show/create/update/destroy to `admin` or `cadastro`, deny everyone
  else (including unauthenticated), following `ApplicationPolicy`'s
  deny-by-default base.
- **`ApplicationController`**: now `include Pundit::Authorization` +
  `rescue_from Pundit::NotAuthorizedError` → redirect to `root_path` with an
  alert. This is the first phase to actually wire Pundit into the request
  cycle (Phase 1 only generated the empty `ApplicationPolicy`).
- **Routes/controllers**: `namespace :cadastro do resources :drivers; resources :trucks end`.
  `Cadastro::BaseController` only handles `authenticate_user!` — the
  role gate is Pundit's `authorize`/`policy_scope` alone (no separate
  hand-rolled role check), including on `index` (`authorize Driver` before
  `policy_scope`, otherwise an unauthorized role would get a 200 with an
  empty list instead of a redirect — `Scope#resolve` returning `.none`
  doesn't raise on its own).
- **Views**: plain ERB CRUD views (index/show/new/edit/_form) under
  `app/views/cadastro/{drivers,trucks}/`, matching the existing minimal
  hardcoded-pt-BR-string style (no i18n scoping introduced). Driver form
  has a `collection_check_boxes :truck_ids` to assign trucks directly.
- **Factories/specs**: `Driver`/`Truck`/`DriverTruck` factories (using
  `CPF.generate` for valid unique CPFs); model specs (validations,
  associations, enum, CPF/phone validation, DB-level encryption assertion,
  plate normalization); policy specs using `pundit-matchers`' `permit`
  matcher (added `require "pundit/rspec"` to `rails_helper.rb`); request
  specs per controller covering unauthenticated (302), wrong-role (302),
  and authorized (200 / correct create+destroy) cases (added
  `Devise::Test::IntegrationHelpers` to `rails_helper.rb` for `sign_in` in
  request specs).

### Deviations / decisions discovered during execution

- **No `Cadastro::DriverTrucksController`** — the plan's file-structure
  list only names `DriversController`/`TrucksController`, so pairing
  management was folded into the driver form via `truck_ids=` rather than
  a separate CRUD screen/policy for `DriverTruck`.
- **`DriverTruck#active` isn't exercised yet.** The `truck_ids=` writer
  hard-deletes unchecked pairs rather than soft-disabling them, so the
  "soft-disable without deleting history" behavior described in
  `docs/plan.md` doesn't apply yet. This matters once `Visit` exists:
  Phase 3's `CheckInService` (upserting `driver_trucks` pairs) is where
  `active`/soft-disable semantics should actually be implemented and
  tested.
- **`cpf_cnpj` gem has no Rails validator built in** (only `CPF`/`CNPJ`
  plain Ruby classes) — added a custom `validate :cpf_must_be_valid`
  calling `CPF.valid?` rather than a one-line `validates :cpf, cpf: true`
  as might be assumed from the gem name.
- Deterministic encryption (`encrypts :cpf, deterministic: true`) was
  necessary (not just `encrypts :cpf`) so that `validates :cpf, uniqueness: true`
  and the DB unique index on `cpf` actually work — non-deterministic
  encryption produces different ciphertext each time, breaking both.

### How to verify

```bash
docker compose run --rm -e RAILS_ENV=test web bundle exec rspec   # 55 examples, 0 failures
docker compose run --rm web bin/rubocop                          # clean except pre-existing Phase 1 offenses
docker compose run --rm web bin/brakeman -q                      # 0 warnings
```

Verified manually end-to-end via `curl` against a running `docker compose up`
stack: unauthenticated request to `/cadastro/drivers` → 302 to sign-in;
signed in as a `fila`-role user → 302 to root (Pundit denial); signed in as
a `cadastro`-role user → 200, and a full create → show → edit (assign a
truck via `truck_ids`) → the association round-trips correctly; confirmed
`cpf`/`phone` are stored as ciphertext in Postgres directly (not just at the
Rails level) while the model still returns plaintext.

---

## Next step

**Phase 3** — Yard check-in: `Visit` model + `CheckInService` + yard
listing, restricted to the `cadastro` role. This is also where
`DriverTruck#active` soft-disable semantics should be revisited (see
deviation note above) when the check-in service upserts pairs. See details
in [`docs/plan.md`](plan.md#build-phases-incremental-milestones).
